import { FC } from 'react';
import { ContactsPage } from '@/app-pages/contacts';
import { ContactsPageWithProvider } from '@/app-pages/contacts/ui/contacts';

const Contacts: FC = () => <ContactsPageWithProvider />;

export default Contacts;
